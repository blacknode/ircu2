{
  "name": "@blacknode/sdk",
  "private": true,
  "version": "0.1.0",
  "description": "Protocol SDK for the blacknode ircd: web, mobile and desktop share this",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "typecheck": "bun run --bun tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
